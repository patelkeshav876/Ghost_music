# ══════════════════════════════════════════════════════════
#  GhostMusic Bot — Python Dependencies
#  Pinned for stability. Python 3.10+ required.
#  Install: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════

# ── Telegram MTProto client ───────────────────────────────
pyrogram==2.0.106
TgCrypto==1.2.5          # C-extension for faster crypto (required by Pyrogram)

# ── Voice chat streaming ──────────────────────────────────
# py-tgcalls is the modern maintained fork (uses NTgCalls)
pytgcalls==1.1.6

# ── Audio download / extraction ──────────────────────────
yt-dlp>=2024.11.4         # keep updating for YouTube compatibility

# ── Database ──────────────────────────────────────────────
motor==3.6.0              # async MongoDB driver
pymongo==4.10.1

# ── HTTP / REST API (stats server) ────────────────────────
aiohttp==3.11.10

# ── Config ────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Optional: Spotify support ─────────────────────────────
spotipy==2.24.0

# ── Optional: Lyrics ──────────────────────────────────────
# (lyrics.py uses aiohttp directly — no extra lib needed)

# ── Utilities ─────────────────────────────────────────────
Pillow==11.0.0            # thumbnail generation

# ── FFmpeg is a system dependency, NOT a pip package ──────
# Install on Ubuntu/Debian:
#   sudo apt-get install -y ffmpeg
# Install on macOS:
#   brew install ffmpeg
